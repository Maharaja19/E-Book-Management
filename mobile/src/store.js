import { configureStore } from '@reduxjs/toolkit';

// For now, we'll create an empty store
// In a real implementation, you would add reducers here
export const store = configureStore({
  reducer: {
    // Add reducers here as needed
  },
});